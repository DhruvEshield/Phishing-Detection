[pytest]
# ml modules use flat imports (`from text_normalize import ...`), so the ml/
# directory must be on sys.path when tests run. `pythonpath = .` puts it there
# regardless of the working directory pytest is invoked from.
pythonpath = .
testpaths = tests
